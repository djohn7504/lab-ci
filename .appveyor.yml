# APPVEYOR_CACHE_SKIP_SAVE: true ?..
# APPVEYOR_CACHE_SKIP_RESTORE

version: 1.0.{build}

branches:
  only:
    - master

test_script:
- cmd: echo "build" %appveyor_build_version%

build_script:
- cmd: echo "build %appveyor_build_version%" >> deploy/index-windows.html

artifacts:
- path: deploy\index-windows.html

deploy:
- provider: GitHub
  on:
    branch: master
  auth_token:
    secure: kO7VTv1Eaoz8h7JS8lZ2KCnd5MoE8hnuUMHvP5KnhrlRAcZiP5MCKlvR/CIbl1qK
  tag: release-test
  force_update: true

after_deploy:
# - sh ./deploy-gh-pages-01.sh  # OK
# - sh ./deploy-gh-pages-02.sh # BAD
# - sh ./deploy-gh-pages-03.sh # ?..

  - ps: git config --global user.email "djohn7504@gmail.com"
  - ps: git config --global user.name "djohn7504"
  - ps: git config --global credential.helper wincred
# - ps: rm -rf out || exit 0;
# - ps: rm -rf out
# - cmd: rmdir out
  - cmd: mkdir "out"
  - ps: ls
# - ps: exit 0
  - cmd: git clone --branch "gh-pages" https://github.com/djohn7504/lab-ci.git "out"
  - ps: cd out
  - ps: echo "Hello Windows (build $APPVEYOR_BUILD_VERSION)" >> index.html
  - ps: git add .
  - ps: git commit -m "Deployed to Github Pages"
  - cmd: git push --force --quiet "https://fe8a4ae5169d802c1b527efa4e6b22349a6e811c@github.com/djohn7504/lab-ci"
